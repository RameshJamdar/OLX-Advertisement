package com.example.datajpa;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.boot.test.autoconfigure.jdbc.AutoConfigureTestDatabase;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.boot.test.context.SpringBootTest;

import com.example.datajpa.entity.ContactMasterEntity;
import com.example.datajpa.repository.ContactMasterRepo;
import com.example.datajpa.service.ContactMasterService;


@DataJpaTest
@SpringBootTest
@AutoConfigureTestDatabase
public class ContactMasterRepoTest {
	
	@Autowired
	private ContactMasterRepo repo;
	
	@Autowired
	private  ContactMasterService service;
	
	@Autowired
	private ContactMasterEntity entity;
	
	@Test
	public void saveContactTest() {
//		ContactMasterEntity entity=repo.findByContactName("Ramesh");
//		assertThat(entity.getContactName()).isEqualTo("Ramesh");
		
		
		  ContactMasterEntity entity= new
		  ContactMasterEntity(101,"Ramesh",9988776655L); 
		   repo.save(entity);
		  Assertions.assertNotNull(entity); // 
		  ContactMasterEntity entity2=repo.findByContactName("Ramesh"); 
		  Assertions.assertEquals(entity2.getContactName(), entity.getContactName());
		  
		 	}

	

}
